{
  "name": "veistgeneratorbot",
  "version": "1.0.0",
  "description": "Bot for the Veist project.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node deploy-commands.js && node index.js",
    "reset-db": "rm -f database.sqlite && touch database.sqlite"
  },
  "author": "Zavier Boyles & Tom White",
  "license": "ISC",
  "dependencies": {
    "@gradio/client": "^1.8.0",
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5",
    "sequelize": "^6.37.5",
    "sqlite3": "^5.1.7"
  }
}
